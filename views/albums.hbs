{{#each items}}
        <div>
            <img src = "{{this.images.url}}">
            <h3>{{this.name}}</h3>
            <a href = "/tracks/{{this.id}}">View Tracks</a>
        </div>
    {{/each}}